# Issue #023 — 串口协议过于简陋，后续需升级

**日期**：2026-03-24
**提出人**：Codex（审查 #020）
**状态**：open
**优先级**：低
**类型**：架构
**涉及文件**：`firmware/motor_control/motor_control.ino`

## 问题描述

当前协议"两字符即命令"，无帧边界、命令 ID、校验、错误码。手动调试够用，但对自动化（断线重连、日志追溯、命令确认）不够。

## 说明

#017 架构方案的策略是把复杂性放在 Python 上位机层，固件保持简单。因此短期内不需要重做协议。但长期看，至少需要加：
- 命令响应关联（发 `M 1000 2000 3000` 能确认是否执行成功）
- 错误码（区分"命令不认识"和"限位触发无法执行"）
- 状态快照查询（一条命令返回全部轴位置+传感器+报警）

## 验收标准

- [ ] 定义 v2 协议规范文档
- [ ] 固件实现并保持向后兼容
