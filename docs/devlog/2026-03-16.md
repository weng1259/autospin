# 开发日志 — 2026-03-16

## 记录人

FENG

## 今日工作

- 决定用命令行工具 `arduino-cli` 替代 Arduino IDE 进行固件开发
- 安装 arduino-cli（`brew install arduino-cli`），配置 Arduino Mega 2560 开发环境
- 编译并上传 LED 闪烁测试程序（blink_test），验证完整工具链：编译→上传→运行

## 遇到的问题

- CH340 国产版 Arduino 在 Mac 上未被自动识别，端口列表为空

## 解决方案

- 安装 CH340 驱动后，端口正常显示为 `/dev/cu.usbserial-110`
- LED 闪烁成功，确认工具链完全可用

## 下次计划

- 硬件接线完成后，用 arduino-cli 编译上传电机控制固件

## 备注（照片/截图/参考链接）

- arduino-cli 版本 1.4.1
- 板型：`arduino:avr:mega`
- 选择 arduino-cli 而非 IDE 的原因：可被 AI 助手直接调用，全程终端操作
